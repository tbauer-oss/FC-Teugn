{
  "version": 2,
  "installCommand": "npx -y pnpm@10.13.1 --dir api install --frozen-lockfile",
  "buildCommand": "npx -y pnpm@10.13.1 --dir api run build",
  "outputDirectory": "dist",
  "functions": {
    "api/index.ts": {
      "includeFiles": "{api/dist/**,api/prisma/**}"
    }
  }
}
