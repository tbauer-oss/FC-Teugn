{
  "version": 2,
  "installCommand": "cd api && corepack enable && corepack prepare pnpm@10.13.1 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd api && pnpm build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "{dist/**,prisma/**}"
    }
  }
}
