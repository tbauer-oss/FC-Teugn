{
  "version": 2,
  "installCommand": "cd api && corepack enable && corepack prepare pnpm@9.12.2 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd api && pnpm build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "{dist/**,prisma/**}"
    }
  }
}
