{
  "version": 2,
  "installCommand": "cd api && npm install -g pnpm@10.13.1 && pnpm install --frozen-lockfile",
  "buildCommand": "cd api && pnpm build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "{dist/**,prisma/**}"
    }
  }
}
