{
  "version": 2,
  "installCommand": "cd api && npm install -g pnpm@9.15.2 && pnpm install --frozen-lockfile",
  "buildCommand": "cd api && pnpm build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "{dist/**,prisma/**}"
    }
  }
}
